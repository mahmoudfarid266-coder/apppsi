# Claude Code — may contain credentials/auth tokens (flagged by Spec Kit)
.claude/settings.local.json
.claude/.credentials.json

# Secrets
.env
.env.*
!.env.example

# Node / Expo
node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo

# Native build output (regenerated by `expo prebuild`)
ios/Pods/
ios/build/
android/build/
android/app/build/
android/.gradle/

# Supabase local
supabase/.branches/
supabase/.temp/

# OS
.DS_Store
Thumbs.db

# Remotion render output
remotion/out/
remotion/node_modules/
